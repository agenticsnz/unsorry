Added the benchmark suite importer (`tools/intake/import_benchmark.py`, ADR-092/M4): turns an external Lean benchmark suite (PutnamBench first) into swarm-visible top-level `goals/<slug>.{aisp,lean}` (via the existing `gen_triples.write_triple`) **and** a self-contained, `skeleton-validate`-admissible `targets/<suite>/` package (synthetic `True` sentinel root, a flat `top → obligation` decomposition, `skeleton.aisp` manifest + ADR-078 credited/glue map, `target.aisp` metadata). Extraction, kebab-slugging, package assembly and the ≤50-obligation batch cap are pure and fully tested; the Lean re-elaboration (quarantine on failure) and the full-battery credited/glue classification are an injectable seam wired to `check_triviality` for a real run. The CLI self-validates the assembled package against `skeleton-validate`. (#5643, M4.)
