**`swarm/agent.sh` is shellcheck-clean again (#7159 follow-up).** The `surface_gate_b_violations` helper added in #7180 took its parameter as `local out="$1"`, but `out` is declared `local -a` in `batch_build_worktree`, so shellcheck's file-scope tracking read the assignment as an array being given a string (SC2178) and every expansion as an unindexed array read (SC2128). The parameter is now `report`. No behaviour change — the helper printed correctly throughout, since the diagnosis was static-analysis scope confusion rather than a real array. Note that `agent-lint` is not a required check, so #7180 merged with this failing; the SPEC-007-A "shellcheck-clean" bar is advisory in practice.
