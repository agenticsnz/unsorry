Corrected stale factual claims in the README's "What this is" and "How the loop works" sections so the homepage matches the current system. The description no longer says Gemini and OpenAI are "local-only" (all four providers — Claude, Codex, Gemini, OpenAI — are accepted for both coordinated and local proving, `swarm/agent.sh`); it no longer implies every proof is LLM-written (a deterministic sympy/template solver discharges elementary goals with no LLM, [ADR-079](docs/adrs/ADR-079-Deterministic-Solver-Provider.md)); "claim an open goal" is now scoped to coordinated mode, since fork-native proving is claimless ([ADR-068](docs/adrs/ADR-068-Fork-Native-Contribution-Mode.md)); and "merge it back" is replaced with the actual flow — a governor-metered dispatcher opens the PR that Gate A re-verifies and auto-merges ([ADR-058](docs/adrs/ADR-058-Runner-Pool-Segmentation-And-Verification-Capacity.md)). The load-bearing claim — no human in the kernel correctness path — is unchanged because it remains true.
