The benchmark importer now bundles each theorem's **companion declarations** (PutnamBench's `abbrev …_solution`, `open` directives, helper `def`s) into the goal, so answer-carrying ("determine the answer") problems import as **fixed, provable proof-track goals** instead of being quarantined for a missing/opaque reference. An answer-blank `:= sorry` is substituted with the answer PutnamBench leaves in the adjacent `-- <answer>` comment; `--build`'s elaborate check still quarantines any substitution that doesn't type-check under the pin. `tools.sourcing.gen_triples.render_lean`/`write_triple` gain an optional `preamble`. (#5643.)
