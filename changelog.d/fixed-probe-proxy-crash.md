Fixed the benchmark importer aborting an entire `--build` batch when one statement's `foralltype` glue-probe proxy can't be constructed (unusual `let`/`letI`-in-binder signatures, e.g. `putnam_1989_a6`, `putnam_2023_a4`). Such a statement **builds** at the suite pin, so it is now credited rather than crashing the batch — which had silently halted the PutnamBench ingestion at 241/672.
