Registered a small **demo benchmark suite** (`targets/demo-v1/`, supplier `agenticsnz`, MIT) to exercise the ADR-092 benchmark track end-to-end: four true Lean statements imported via `tools.intake.import_benchmark` as a `skeleton-validate`-admitted package + swarm-visible open goals (`demo-add-comm`, `demo-two-add-two`, `demo-mul-comm`, `demo-le-refl`). It surfaces on the unsorry-guild `/math/goals` Benchmark-suites section with copy-paste `run.sh --goal <id>` lines, segregated from the organic leaderboard. Safe to remove once the real suites land. (#5643.)
