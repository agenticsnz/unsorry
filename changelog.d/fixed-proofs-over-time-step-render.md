Fixed the `docs/proofs-over-time.svg` cumulative line to render as a **step** (hold the prior level flat between buckets, then jump when proofs land) instead of a straight diagonal between sparse points. A cumulative count is a step function, so a no-proof gap — e.g. the 2026-06-26→06-29 drought — now reads as *flat-then-jump* ("no growth, then resumed") rather than a diagonal that misleadingly implies steady growth across days where nothing was proved. Complements the ADR-111 trailing carry-forward; the dashed trailing segment and edge tick are unchanged. (ADR-111 follow-up.)
