Documented the existing single-goal flow: `./swarm/run.sh --goal <id>` (or `./swarm/supervise.sh --prove --goal <id>`) proves one specific goal — a kebab slug, or a benchmark goal once suites are registered — plus any sub-lemmas it decomposes into, then exits cleanly once the goal's scope is fully proved. `scope_closed` termination and `run.sh`'s background-dispatcher cleanup are built in, so the contributor "copy id → run" benchmark flow needs no extra flag. (#5643, M6.)
