Retired the `demo-v1` smoke-test suite from the guild goals page now that the real **PutnamBench** benchmark (`putnam-v1`) is live: marked it `retired` in `docs/governance/admitted-domains.json` so the generator drops it from the published `registered-targets.json` (the guild stops listing it). Its goals are immutable (ADR-018), so they stay in the repo and remain benchmark-segregated — never re-counted on the organic board. (#5643.)
