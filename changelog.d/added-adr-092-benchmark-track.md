Accepted **ADR-092 (Segregated Benchmark Track)** + **SPEC-092-A**: external Lean benchmark suites (PutnamBench, miniF2F, CombiBench, curated historically-hard statements) are ingested as ADR-081 skeleton packages registered as ADR-078 targets, scored as **verified pass@k** (every counted pass is a kernel-verified Gate A merge — "0 false positives"), and rendered on a segregated `cohort:benchmark` surface (`docs/metrics/registered-targets.json`) that is excluded from the organic leaderboard. A contributor drives one goal via `./swarm/run.sh --goal <id>`. SWE-bench Lite stays out of scope by design (test-oracle, ADR-080). (#5643, M5.)
