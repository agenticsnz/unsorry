The leaderboard model distribution now folds deterministic-template provenance to its honest engine (`lean/decide`, `lean/ring`, `python/sympy`) at generation time, so a proof landed before the next attribution sweep never surfaces a phantom `claude / template-*` model. The sweep's lean-template rules are also now tactic-suffix tolerant, so a new template name (e.g. `template-fin-decide`) is caught automatically (ADR-100).
