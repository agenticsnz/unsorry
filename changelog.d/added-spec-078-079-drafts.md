Added SPEC-078-A (Structural Scoring) and SPEC-079-A (Domain-Admission Registry and Gating Predicate) as drafts, completing the spec-per-ADR requirement (ADR-028/061) for the ADR-078/079/080 draft set. SPEC-078-A specifies the depth/fan-in scorer over a curated package's decomposition DAG (curated-only, over-decomposition discounted, plugs into tools/leaderboard). SPEC-079-A specifies the machine enforcement of ADR-079's gating invariant: an auditable `admitted-domains.json` registry + pure `domain_admissible`/`target_curated` predicates that the intake (SPEC-080-A) and scoring (SPEC-078-A) paths consult, so "kernel-grade verifier only" is a test, not a footnote.
