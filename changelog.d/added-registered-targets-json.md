Added the `docs/metrics/registered-targets.json` generator (`tools/leaderboard/registered_targets.py`, SPEC-092-A): publishes the registered benchmark suites + per-goal `run_snippet` (the unsorry-guild goals-page *intent* surface), computed deterministically from `targets/<suite>/` and `library/index`. Wires **cohort segregation** into the leaderboard — registered benchmark obligations are excluded from the organic board, a no-op until a suite lands so organic stats stay byte-identical — and ships the unbiased `pass_at_k` estimator. (#5643, M5.)
