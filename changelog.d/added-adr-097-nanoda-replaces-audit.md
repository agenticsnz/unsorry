Proposed **ADR-097 / SPEC-097-A** (Phase 3b): replace the Gate A axiom audit (~55–65% of per-PR runner-min, 8 concurrent shards) with the nanoda scoped-export check, while keeping the `leanchecker` kernel replay at `p = 1` and the ADR-011 binding gate. nanoda already enforces the axiom whitelist and rejects a sneaked `sorryAx` (validated by ADR-096 acceptance gates 1–2) in ~seconds on one runner, so it subsumes the audit's job without changing the kernel truth oracle. Expected ~2× throughput from halving the per-PR runner footprint; the remaining path to ~3–4× (sampling the replay at `p < 1`) is deferred to a future Phase 3c ADR amending ADR-049. Staged: nanoda runs non-required alongside the audit first, then the cutover (explicit maintainer go).
