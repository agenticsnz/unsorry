Proposed **ADR-097 / SPEC-097-A** (Phase 3b): replace the Gate A axiom audit with the nanoda scoped-export check, **keeping the leanchecker replay at `p = 1`**. The audit is ~55–65% of per-PR runner-min and 8 concurrent shards; nanoda enforces the same axiom whitelist (rejecting a sneaked `sorryAx`) in ~seconds on one runner, so dropping the audit roughly halves the per-PR runner footprint → ~2× the Namespace-bound merge-rate ceiling — without amending ADR-049's kernel invariant (leanchecker still re-checks every proof). The ADR-011 binding gate is retained; a daily full-audit backstop + a sampled per-PR real audit catch any nanoda under-enforcement. Staged: nanoda as a non-required job first, then an explicit-go cutover. Sampling/replacing the replay at `p < 1` is left to a future Phase 3c.
