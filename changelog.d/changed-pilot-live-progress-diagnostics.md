Improved the D2 export-checker pilot's observability (ADR-093): the driver now emits **live per-module progress** to `GITHUB_STEP_SUMMARY` (and flushed stdout) as each module finishes — so a long pilot run is observable in real time instead of opaque until the step ends — and it **captures the checker's stderr** for any non-ok module, surfacing it in a "nanoda diagnostics" section of the report. This closes the gap that left run 1/2's instant nanoda errors un-diagnosable (stderr was discarded). Observe-only; gates nothing. Part of #5684.
