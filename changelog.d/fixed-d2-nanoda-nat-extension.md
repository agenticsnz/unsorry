Fixed the D2 pilot's nanoda config to enable `nat_extension` and `string_extension` (ADR-093). Run 3's live diagnostics (newly captured) showed nanoda hard-erroring instantly with *"Nat lit extension disallowed by checker execution config, but export file contains a nat literal"* — these options default to false in nanoda but every real Lean export contains Nat/String literals, so they must be enabled to check anything non-trivial (the README's example config sets both true). This was a config gap, not a format incompatibility — nanoda can check our exports. Unblocks the Q3 wall-clock measurement. Observe-only; gates nothing. Part of #5684.
