Added the `skeleton-validate` CI workflow (`.github/workflows/skeleton-validate.yml`): on any PR touching `targets/**` it runs the ADR-081 intake validator (`skeleton-validate --strict`, structural checks 1–5) on every registered benchmark/target package, rejecting an ill-formed package at the door; when `tools/intake/**` changes it also runs the intake + governance test suites. Mirrors Gate B's always-reports short-circuit so it is safe to make a required check. (#5643, M4.)
