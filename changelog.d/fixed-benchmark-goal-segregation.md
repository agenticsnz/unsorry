Fixed a gap in the per-suite mathlib pin track (ADR-099): benchmark obligation **statements** were still written to top-level `goals/`, which the v4.30 `UnsorryGoals` build globs — so a goal recovered at a suite's native pin (e.g. v4.24) would fail `gate-a-prepare` (#6381). Per **ADR-110 / SPEC-110-A**, the importer now relocates the swarm-visible statement to `benchmark-goals/<slug>.lean` (module `benchmark_goals.<slug>`, outside the `goals.+` glob); it is elaborated and kernel-verified only at the suite pin in `targets/<suite>/_verify`. `registered-targets.json` reads obligation difficulty from either dir. Unblocks the native-pin batches for #5643.
