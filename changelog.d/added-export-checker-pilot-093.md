Added the Phase-3 pilot harness (ADR-093 / SPEC-093-A, D2): `tools/pilot/export_checker_pilot.py` plus the observe-only `export-checker-pilot.yml` workflow (`workflow_dispatch`, non-gating, read-only) measure **`lean4export` cross-run determinism (Q2)** and **`nanoda` wall-clock / timeout vs `leanchecker` (Q3, with a >100× pathology flag)** on a sample of real library closures, emitting `pilot-report.{json,md}`. It admits no content and leaves the authoritative `leanchecker` gate untouched; a `nanoda` incompatibility is a recorded Q3 result, not a failure. The data feeds a future Phase-3 decision on whether a cheap mathlib-free re-check — the only verification-throughput *slope*-changer (#5656 Track B) — is viable. Part of #5684 (roadmap #5678).
