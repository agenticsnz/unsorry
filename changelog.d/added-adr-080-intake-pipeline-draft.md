Added ADR-080 (Draft) — Problem Admission and the Skeleton Intake Pipeline. Operational companion to ADR-079: defines how an admissible problem becomes pipeline-consumable work. Specifies the intake funnel (formalise top statement → architect skeleton → emit goal records → pin verifier context → swarm consumes), a checkable admissibility test, and a new `skeleton-validate` intake gate (top statement type-checks; every leaf is a well-formed open `sorry` goal; decomposition edges sound + acyclic; verifier context resolves; curated-target provenance present). Completes the set: ADR-078 (what counts) · ADR-079 (what's admissible) · ADR-080 (how it's consumed). Lion is the first package to run through it.
