Fixed the D2 pilot's `nanoda` invocation (ADR-093). Run 1 had `nanoda` erroring instantly on all 8 modules — diagnosed as a harness bug, not a `nanoda` limitation: the crate's binary is `nanoda_bin` (not `nanoda`) and it takes a single argument that is a **JSON config file** (there is no CLI), so passing the export file as argv made it try to parse a multi-GB export *as config* and hard-error. The driver now writes a per-module nanoda config (pointing at the export, permitting the audit whitelist `{propext, Classical.choice, Quot.sound}` + `Lean.trustCompiler`, with `unpermitted_axiom_hard_error: false`) and invokes `nanoda_bin <config>`; the workflow builds `--bin nanoda_bin` explicitly. Observe-only; gates nothing. Unblocks the Q3 (independent-checker wall-clock) measurement.
