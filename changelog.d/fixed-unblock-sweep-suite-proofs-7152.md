**The unblock/recompose sweep now sees proofs made at a suite pin (ADR-099/ADR-116, #7152).** `_proved_goals` read only the repo `library/index`, but a benchmark obligation and — through the decomposition graph — its sub-lemmas prove at their suite's pin, so their index entries land in `targets/<suite>/_verify/library/index`. Every unblock and recompose call site passes the repo library, so a parent whose sub-lemmas were all proved at the suite pin looked permanently unproved: it stayed `blocked` and was never re-opened, `recompose-candidate` reported no, the ADR-115 assembly steering block never printed, and the ADR-034 demote floor never engaged. Observed on `aime-1983-p9-s1`, whose two sub-lemmas both merged `proved` while the parent stayed `blocked`. `_proved_goals` now unions the repo index with every `targets/*/_verify/library/index`, mirroring `tools.leaderboard.registered_targets._proved_goal_ids` and Gate B's `_proof_index_exists`; suites are resolved relative to the library's parent, so hermetic trees without a `targets/` directory are unaffected. This is the same single-index assumption previously fixed in Gate B — the fourth component to carry it.
