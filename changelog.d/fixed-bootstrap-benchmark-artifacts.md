Bootstrapped `docs/metrics/registered-targets.json` (new `top`/`run_snippet`/`stats` schema, putnam-v1 only) and the new `docs/metrics/benchmark-runs.json` directly, because the leaderboard refresh livelocked under the prove-firehose and hadn't regenerated them after #6274. Deterministic generator output (matches what the workflow produces); the guild benchmark cards/detail page light up immediately. (#5643.)
