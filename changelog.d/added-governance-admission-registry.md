Added the domain-admission registry (`docs/governance/admitted-domains.json`) and its gating predicate (`tools/governance/admission.py`), implementing SPEC-080-A. `domain_admissible()` is true only for `tier: VERIFIED` kernel-grade domains (ADR-080 clause 1 — nothing softer joins the trustless commons), and `target_curated()` returns the vetted supplier for a registered package (self-minted → `None`). `lean-math` is admitted at VERIFIED. This is the single source of truth the intake validator (SPEC-081-A) and the credit pass (SPEC-078-A) consult; `python3 -m tools.governance` validates the registry. (#5643, M2.)
