The sub-lemmas of the benchmark obligation `aime-1983-p9` (minif2f-v1) — proved at the *repo* pin (`lean4:v4.30`/mathlib `c5ea003`) before ADR-116 and therefore unusable for the suite-pinned recompose — are re-opened (their repo-library modules + index entries removed) so the swarm re-proves them at the **suite pin** (`lean4:v4.24`/mathlib `f897ebcf`), where the parent's recompose can import them. Pilot migration for ADR-116; the remaining pre-ADR-116 putnam-v1 obligations in the same state follow once this loop is confirmed.
