Fixed the post-merge leaderboard refresh so it actually publishes `docs/metrics/registered-targets.json`. The M5a generator added the artifact to its `--write`/`--write-if-stale` set, but `leaderboard.yml`'s commit step stages a hardcoded file list that omitted it — so the file was regenerated and then discarded (via `git reset --hard`) on every run and never reached `main`, leaving the unsorry-guild benchmark-suites section empty. Added `docs/metrics/registered-targets.json` to the workflow's `git add` list. (#5643.)
