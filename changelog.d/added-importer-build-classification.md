Made the benchmark importer turnkey: `python3 -m tools.intake.import_benchmark … --build` now elaborates each statement under the pinned mathlib and (1) **quarantines** the ones that don't elaborate (reported, never imported — handling mathlib-pin drift automatically) and (2) classifies each kept obligation **credited/glue** via the ADR-078 full battery. So a real suite (PutnamBench, miniF2F) imports in one command instead of needing a manual `lake build` filter pass. The Lean probe is an injectable seam (wired to `check_triviality.probe`), so the partition logic is fully unit-tested without Lean. (#5643, M4.)
