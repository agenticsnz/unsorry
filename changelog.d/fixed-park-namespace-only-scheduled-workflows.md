**Three scheduled workflows are parked instead of queuing forever (ADR-117 follow-up).** `lake-volume-janitor`, `gate-a-full-replay` and `independent-check-backstop` all target `namespace-profile-unsorry-1`, which has had zero runners since the namespace.so trial ended, so every daily cron since then has queued indefinitely and accumulated stuck runs. None gates a merge, so nothing was blocked — but the crons made three dead workflows look alive. Their `schedule:` triggers are removed and `workflow_dispatch` is kept, with each one now stating what it would take to bring it back. **`gate-a-full-replay` is the one that matters**: it is the ADR-048 Phase 2 defense-in-depth backstop that re-runs a full kernel replay and full axiom audit over the entire active library, and until it is retargeted the residual bookkeeping risk it covers is uncovered. It was already dead; the cron only disguised that. None of the three is a label swap away from working — all attach `nscloud-cache-action`, which only functions on a Namespace runner, so each needs the volume-vs-`actions/cache` treatment `gate-a.yml` received, and the full replay additionally needs a cost assessment against its 180-minute cap on a 4-vCPU runner.
