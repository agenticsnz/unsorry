Added `skeleton-validate` (`tools/intake/skeleton_validate.py`), the curated-package intake validator (ADR-081 / SPEC-081-A). `python3 -m tools.intake.skeleton_validate <pkg> [--strict] [--build] [--json]` admits or rejects a skeleton package: manifest well-formedness, vetted-supplier provenance (via the SPEC-080-A registry), well-formed open-goal obligations, sound acyclic decomposition edges rooted at the top with no orphans, and no degenerate pass-through padding (checks 1–5, hermetic). `--build` adds the Lean top-statement type-check and the ADR-078 full-battery credited/glue classification (checks 6–7). Exit 0 admit / 1 reject / 2 error, mirroring Gate B; reuses the shared `tools/gate_b/graph.py` and `tools/governance/admission`. (#5643, M3.)
