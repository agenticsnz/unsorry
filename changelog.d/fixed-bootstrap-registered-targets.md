Bootstrapped `docs/metrics/registered-targets.json` directly so the unsorry-guild benchmark-suites section can render the registered `demo-v1` suite immediately. The post-merge `leaderboard.yml` refresh (which now also publishes this artifact) was lagging under the prove-merge firehose and hadn't written it yet; the committed file is deterministic, so a future refresh produces the identical content (or updates `proved` as goals close). (#5643.)
