Factored the decomposition-graph helpers (the sub/edge regexes and the DAG cycle check) out of the Gate B validator into a shared `tools/gate_b/graph.py`, so the upcoming curated-package intake validator (`skeleton-validate`, SPEC-081-A) reuses the exact same logic rather than copying it (DRY). Gate B behaviour is unchanged — the validator keeps its `_SUB_RE`/`_EDGE_RE`/`_has_cycle` names as thin aliases. (#5643, M2.)
