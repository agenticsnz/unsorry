Fixed the lead-engineer credit typo in `README.md` (`perrtu` → `perttu`; the link URL was already correct). Fixed overlapping avatars on the leaderboard's top progress strip (`docs/leaderboard.html`): avatars are placed at `left: score%`, so a dominant leader compressed every other contributor into the left edge and piled the avatars on top of each other — they are now spread with a minimum horizontal gap (placed high-to-low score, each kept at least 4% left of the previous), so the cluster reads cleanly.
