**ADR-104 / SPEC-104-A — Phase 3c: sampled leanchecker replay (design banked, ships disabled).** Specifies an operator knob `vars.UNSORRY_REPLAY_SAMPLE` (default `1.0` — a no-op) that would sample the central `leanchecker` kernel replay at `p < 1`, with nanoda (ADR-097) kernel-checking every proof, content-hash-deterministic unbiased selection, disagreement-halts-the-swarm, fail-closed, and the ADR-048 daily backstop re-checking 100% within 24 h. Amends ADR-049's `p = 1` invariant. Enablement is explicitly gated (G1 production agreement, G2 deepened nanoda kernel review, G3 verified sampling, **G4 verification is the binding constraint**) behind a separate code-owner PR — landing the ADR changes no runtime behavior. Refs #5678, #5684.
