Hardened and broadened the D2 scoped-export pilot (ADR-093, #5684): the driver now (1) **records nanoda's declaration count** (`Checked N declarations`) and **confirms the target theorem is in the checked environment** via `pp_declars` + `unknown_pp_declar_hard_error` — guarding the degenerate-pass risk that a declaration-scoped export might check only dependencies, not the proof; and (2) adds a **`--spread` sample** that draws modules evenly across the library (topic/difficulty diversity) instead of the homogeneous first-N. Together these let a scoped run *prove* the check is real and test how export size + nanoda speed scale across diverse proofs. Observe-only; gates nothing.
